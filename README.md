# Ajax_Cards_Application
 The application creates product cards dynamically, loading them from the server using AJAX using GET and POST requests. It also allows you to load information from the server.
